def N<=3;
* A{N+1} = a.A{N} + b.0;
* A{0} = b.0;
A{N+1} = a.nil + b.nil;
A{N-1} = a.(nil + b.nil);
A{N} = a + b;
A{0} = skip ;
def b2=[true,false];
def k=[1,2];
*A = while (b2 && k==2) do skip;
B = x := 3;
C = if (x > 3) then skip else skip ;
set L = { val, x, y};