def N<=3;
def c <= 3;
A{N+1} = a.A{N} + b.0;
A{0} = b.0;
C = a.A + b.B;
def b2=[true,false];
def k=[1,2];
def x = [3, 7, 9];
A = while (b2 && k==2) do {skip};
*A = a(x).('b<x>.A + 'c<0>.0);
B = x := 3;
C = if (x == 3) then {skip} else {skip} ;
*set L = { val, x, y};
*C1 = C \ L ;
*A1 = (A \ {a,b}) \{c};
*P1 = P[c/a,d/c];
MT1 = 'exit1.MT + 'enter2.'bad.0   ;
